# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit==1.43.2

# ── PDF → DOCX conversion (hybrid PyMuPDF + pdfplumber, Linux/Docker safe) ───
pdfplumber==0.11.4

# ── Core pipeline dependencies ─────────────────────────────────────────────────
python-docx==1.1.2
Pillow==11.2.1
requests==2.32.3
lxml==5.3.0
PyMuPDF==1.25.3

# ── LLM / AI ──────────────────────────────────────────────────────────────────
anthropic==0.49.0
openai==1.75.0
chromadb==1.5.5

# ── Data / utilities ──────────────────────────────────────────────────────────
pydantic==2.11.3
pandas==2.2.3
openpyxl==3.1.5

# ── FRS14 HTTP bridge (Windows-side server) ───────────────────────────────────
flask==3.1.3
waitress==3.0.2

# ── HTTP client (used by streamlit_app._pdf_to_docx and anthropic SDK) ────────
httpx==0.28.1

# ── NOTE: pywin32 (win32com) is intentionally excluded ────────────────────────
# It is Windows-only and not available in the Linux Docker container.
# The old ABBYY COM API is replaced by the FRS14 HTTP bridge (frs14_bridge.py).
